/** @type {import('next').NextConfig} */
const nextConfig = {
    
     // next.config.js

    images: {
      domains: ['images.pexels.com','images.unsplash.com'],
      
    },
 
  
        // Add other image domains you'll use

};

export default nextConfig;
